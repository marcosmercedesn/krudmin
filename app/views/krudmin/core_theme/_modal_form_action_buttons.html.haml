- configure_toolbar(:form, self) do |b|
  - b.button :save, form_submit_path, form: f
  - b.button :cancel, '#', data: {bs_dismiss: :modal, type: :button}
  - b.button :show, model, data: { controller: "remote-modal", action: "click->remote-modal#open" }